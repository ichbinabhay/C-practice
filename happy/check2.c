#include<stdio.h>
#include<math.h> 
int main() {
    int isSunday=1;
    int isSnowing=1;
    printf("%d \n", isSunday && isSnowing);
    return 0;
} //here the program is having sunday as true(1) and snowing is also true(1) so final result is also is true 
 