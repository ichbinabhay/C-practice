#include<stdio.h>
#include<math.h>
int main() {
    printf("%d", 8^8);
    int x; int y=x; //checking whether this statement is valid and it is valid
    return 0;
}
// in this program we are checking whether 8^8 is valid or not , in result to that we get to know that its is valid code but it is not code for getting power of 8
