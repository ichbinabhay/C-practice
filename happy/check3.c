#include<stdio.h>
#include<math.h> 
int main() {
    int isSunday=1;
    int isSnowing=1;
    printf("%d \n", isSunday || isSnowing);
    return 0;
} //here the program is having sunday as true(1) and snowing is also true(1) so final result depends on the relation btw them whichh is or(||) so both statements are true so result is also true 
  
 