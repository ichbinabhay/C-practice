#include<stdio.h>
#include<math.h> 
int main() {
    int isSunday=0;
    int isSnowing=1;
    printf("%d \n", isSunday || isSnowing);
    return 0;
} //here the program is having sunday as false(0) and snowing is  true(1) so final result depends on the relation btw them whichh is or(||) so one is false and one is true  so result is  true because in or if we one is T and other is F so we will consider T 
 